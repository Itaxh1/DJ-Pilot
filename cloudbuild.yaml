# Google Cloud Build configuration
steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/$PROJECT_ID/livestream-audio:$COMMIT_SHA',
      '-t', 'gcr.io/$PROJECT_ID/livestream-audio:latest',
      '.'
    ]

  # Push the image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/livestream-audio:$COMMIT_SHA']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/livestream-audio:latest']

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: [
      'run', 'deploy', 'livestream-audio',
      '--image', 'gcr.io/$PROJECT_ID/livestream-audio:$COMMIT_SHA',
      '--region', 'us-central1',
      '--platform', 'managed',
      '--allow-unauthenticated',
      '--port', '8080',
      '--memory', '1Gi',
      '--cpu', '1',
      '--max-instances', '10',
      '--set-env-vars', 'NODE_ENV=production,MAX_ROOMS=200,ROOM_TIMEOUT_HOURS=12'
    ]

# Store images in Container Registry
images:
  - 'gcr.io/$PROJECT_ID/livestream-audio:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/livestream-audio:latest'

# Build options
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
